# Library Directory

Place third-party dependencies required by the application in this folder.

## Required JARs
- `opengauss-jdbc-<version>.jar` – the openGauss JDBC driver used by the application.
- JavaFX modules (already included).

> Remove any MySQL driver JARs from this directory to avoid classpath conflicts.
